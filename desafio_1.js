let numeroUm = 1;
let stringUm = '1';

if (numeroUm == stringUm) {
    document.getElementById("resultado1").textContent ="As variáveis 1 e '1' tem o mesmo valor, mas tipos diferentes";
    } else {
    document.getElementById("resultado1").textContent ="As variáveis 1 e '1' não tem o mesmo valor";
    }
if (numeroUm === stringUm) {
    document.getElementById("resultado4").textContent ="As variáveis 1 e '1' tem o mesmo valor, mas tipos diferentes";
    } else {
    document.getElementById("resultado4").textContent ="As variáveis 1 e '1' não tem o mesmo valor";
    }
let numeroDez = 10;
let stringDez = '10';
if (numeroDez == stringDez) {
    document.getElementById("resultado2").textContent ="As variáveis 10 e '10' tem o mesmo valor, mas tipos diferentes";
    } else {
    document.getElementById("resultado2").textContent ="As variáveis 10 e '10' não tem o mesmo valor";
    }
if (numeroDez === stringDez) {
    document.getElementById("resultado5").textContent ="As variáveis 10 e '10' tem o mesmo valor, mas tipos diferentes";
    } else {
    document.getElementById("resultado5").textContent ="As variáveis 10 e '10' não tem o mesmo valor";
    }
let numeroTrinta = 30;
let stringTrinta = '30';
if (numeroTrinta == stringTrinta) {
    document.getElementById("resultado3").textContent ="As variáveis 30 e '30' tem o mesmo valor e mesmo tipo";
    } else {
    document.getElementById("resultado3").textContent ="As variáveis 30 e '30' não tem o mesmo tipo";
    }
if (numeroTrinta === stringTrinta) {
    document.getElementById("resultado6").textContent ="As variáveis 30 e '30' tem o mesmo valor e mesmo tipo";
    } else {
    document.getElementById("resultado6").textContent ="As variáveis 30 e '30' não tem o mesmo tipo";
    }